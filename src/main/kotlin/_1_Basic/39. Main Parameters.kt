// you can edit the args, open the edit run/debug this file, click edit configuration, and then edit the program arguments

fun main(args: Array<String>) {
    for (value in args) {
        println(value)
    }
}